# Momo Alexa Skill — Lambda Environment Variables
# Set these in the AWS Lambda console under Configuration → Environment variables

# Base URL of your Momo instance (no trailing slash)
# Default: https://momotask.app
MOMO_API_BASE_URL=https://momotask.app
